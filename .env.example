# Backend storage mode: 'db' (Heroku Postgres) or 'excel' (OneDrive/Graph)
STORAGE_BACKEND=db

# CORS: allow Netlify site to call the API
ALLOWED_ORIGINS=https://SEU-SITE-NETLIFY.netlify.app

# Optional: basic auth for the server-side UI (ui_app.py)
#UI_BASIC_USER=admin
#UI_BASIC_PASS=trocar123

# Excel integration (only if STORAGE_BACKEND=excel)
#TENANT_ID=
#CLIENT_ID=
#EXCEL_RELATIVE_PATH=
#EXCEL_TABLE_NAME=Orcamentos

# Local testing convenience
#CLOUD_API_DEFAULT_URL=http://localhost:8000

# Database URL is provided automatically by Heroku Postgres add-on in production
#DATABASE_URL=postgres://user:pass@host:5432/dbname
